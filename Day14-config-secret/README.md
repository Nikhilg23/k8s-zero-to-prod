# Day 14 – ConfigMap & Secret

- Created ConfigMap for non-sensitive configuration
- Created Secret for sensitive data
- Injected both into Deployment using env.valueFrom
- Verified environment variables inside the pod
