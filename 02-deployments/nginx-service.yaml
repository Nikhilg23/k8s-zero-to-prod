    apiVersion: v1
    kind: Service
    metadata:
      name: nginx-service
    spec:
      type: ClusterIP      # service type is ClusterIP why because it is only accessible within the cluster privately with out exposing it to the outside world.  
      selector:
        app: nginx
      ports:
        - port: 80
          targetPort: 80